
<p>
  <strong>Event Name:</strong>
  <%= @event.name %>
</p>

<div id="questions">
  <%= render @event.questions %>
</div>

<% if current_user.id == @event.user.id %>
  <%= link_to 'Edit', edit_event_path(@event) %> |
<% end %>
<%= link_to 'Back', pages_myevents_path %>
